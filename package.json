{
  "name": "package-json-in-dist",
  "description": "Utility that copies and update package.json in dist folder to publish only this directory",
  "version": "0.1.0",
  "type": "module",
  "bin": "./src/index.js",
  "packageManager": "yarn@4.5.2",
  "scripts": {
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
